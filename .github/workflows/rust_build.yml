name: Rust Build

on:
  push:
    branches: [ master ]
  # pull_request:
  #   branches: [ main ]

jobs:
  build:
    env:
      PLUGIN_NAME: The_WuBor_Patch
    runs-on: ubuntu-latest
    container: 
      image: jugeeya/cargo-skyline:2.1.0-dkp
    steps:
    - uses: actions/checkout@v2
    - name: Update STD
      run: PATH=$PATH:/root/.cargo/bin:/opt/devkitpro/devkitA64/bin /root/.cargo/bin/cargo-skyline skyline update-std
    - name: Attempt to build
      run: PATH=$PATH:/root/.cargo/bin:/opt/devkitpro/devkitA64/bin /root/.cargo/bin/cargo-skyline skyline build --release
# uncomment the build step below to build on every change to master.
#    - name: Update Release
#      env:
#          PLUGIN_NAME: 'libThe_Bor_Patch.nro'  #  change this to the name of your plugin
#      uses: meeDamian/github-release@2.0
#      with:
#        token: ${{ secrets.GITHUB_TOKEN }}
#        prerelease: true
#        allow_override: true
#        gzip: false
#        tag: master
#        name: master
#        body: Build of ${{env.PLUGIN_NAME}} from master.
#        files: >
#          ./target/aarch64-skyline-switch/release/${{env.PLUGIN_NAME}}
# test
